# قواعد التعاون في TIA Studio

هذا المستودع يحتوي على مصدر **TIA Studio** لتحليل التأخيرات. الهدف من القواعد دي إن أي وكيل أو مطوّر خارجي، بما فيهم z.ai، يقدر يساهم من غير ما يكسّر نسخة مستقرة أو يخلط بيانات مشروع حقيقية مع الكود.

## القاعدة الأهم

> ممنوع الشغل أو الـ push المباشر على `main`.

أي تعديل يتعمل في فرع مستقل ويتراجع في Pull Request قبل الدمج. الأفضل تسمية الفروع بشكل يوضح المهمة، مثل `feature/xer-viewer-filter` أو `fix/windows-startup` أو `docs/release-1.0.8`.

## طريقة الشغل المقترحة

| المرحلة | المطلوب | لا تعمل إيه |
|---|---|---|
| البداية | اسحب آخر `main` وأنشئ فرعاً جديداً | لا تعيد كتابة تاريخ `main` ولا تستخدم `git reset --hard` |
| التنفيذ | عدّل أصغر مجموعة ملفات تحقق المطلوب، وحافظ على حسابات CPM/TIA الحتمية | لا تغيّر منطق الحساب أو العلاقات من غير اختبار صريح |
| الفحص | شغّل `pnpm test` ثم `pnpm check` ثم `pnpm build` | لا تعتبر لقطة واجهة بديلاً عن الاختبارات |
| المراجعة | افتح Pull Request يشرح المشكلة والحل والاختبارات والحدود | لا تدمج Pull Request غير مراجع |
| الدمج | يدمجه مسؤول المشروع فقط بعد نجاح الفحوصات | لا تدفع إلى `main` من وكيل خارجي |

## حماية البيانات والحدود المهنية

لا ترفع أبداً ملفات العميل الخام مثل `.xer` أو `.xml` أو `.xlsx` الخاصة بالمشروع، أو أدلة المطالبة، أو ملفات `.env`، أو بيانات اعتماد، أو مخرجات مشروع حساسة إلى GitHub أو ضمن حزمة المصدر. استخدم بيانات مصطنعة صغيرة للاختبارات فقط، واحتفظ بالملفات الحقيقية محلياً عند المستخدم.

لا يصح أن يدّعي أي تعديل أن الأداة بديل متكافئ لـ Primavera P6 أو أن النتيجة تمنح استحقاقاً قانونياً تلقائياً. أي تصدير XER أو نتيجة TIA تحتاج مراجعة على نسخة غير إنتاجية في P6، مع مراجعة التقويم والعلاقات والـ Data Date والمسار الحرج.

## قبل فتح Pull Request

- اشرح التغيير بالعربي البسيط، واذكر الملفات التي لمستها.
- اذكر الأوامر التي نجحت، وأي تحذير متبقٍ أو بند يحتاج اختباراً يدوياً.
- حدّث `todo.md` بحيث البنود المكتملة فقط تكون `[x]`، ولا تحذف البنود القديمة.
- حدّث الدليل أو ملاحظات الإصدار إذا تغيّرت طريقة الاستخدام أو رابط تنزيل أو حد مهني.
- لا ترفع ملفات `node_modules` أو `dist` أو `release` أو ملفات المستخدم الخام.

## إعدادات GitHub المقترحة للمالك

الفرع `main` في وقت كتابة هذه الوثيقة **عام وغير محمي**. يوصى بأن يفعّل المالك Branch Protection أو Ruleset على `main`: طلب Pull Request واحد على الأقل، نجاح فحوصات `pnpm test` و`pnpm check` و`pnpm build`، منع force-push، ومنع الحذف. يفضل جعل المستودع خاصاً إذا كان سيحتوي على سياق عميل أو محتوى غير مخصص للنشر العام.
