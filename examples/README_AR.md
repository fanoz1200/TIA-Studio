# أمثلة قابلة للتجربة في TIA Studio

تضم هذه المجلدات بيانات تعليمية فقط؛ لا تمثل مشروعاً حقيقياً أو برنامجاً معتمداً. استوردها في نسخة تجريبية من التطبيق، ثم قارن المخرجات بالنتائج المشروحة هنا.

| الملف | الغرض | كيفية استخدامه |
|---|---|---|
| `01-baseline-schedule.json` | برنامج أساس من أربع أنشطة مع موارد | استورده من استيراد JSON واحفظ الإكمال الأساسي |
| `02-update-after-foundation.json` | تحديث لاحق يطيل نشاط الأساسات | استخدمه في لوحة مقارنة التحديثات |
| `03-foundation-delay-event.json` | Fragnet لحدث تأخير حفريات | انسخ معلوماته إلى نموذج الحدث بعد استيراد الأساس |
| `04-minimal-p6-resource.xer` | XER صغير يثبت قراءة `TASKRSRC` | استورده في تبويب XER وراجع المورد والتكلفة |
| `05-training-tia-baseline.json` | برنامج TIA مصطنع من أربعة أنشطة على مسار حرج واحد | استورده ثم طبّق ملف الحدث رقم 06؛ الأثر المتوقع +4 أيام عمل |
| `06-training-tia-event.json` | Fragnet لاعتماد حفر متأخر لمدة 4 أيام | استبدل العلاقة `TR-200` بالـFragnet بعد اعتماد Pre-TIA |
| `07-training-concurrency-baseline.json` | برنامج مصطنع بمسارين متوازيين حرجين | استورده لتدريب تحليل التزامن، لا لاستخلاص حكم مسؤولية |
| `08-training-concurrency-events.json` | حدثان تدريبيان متزامنان، كل منهما 3 أيام | أدخل الحدثين منفصلين؛ الأثر الشبكي المدمج المتوقع +3 لا +6 أيام |

## سيناريو التدريب

ابدأ بملف الأساس، وتأكد من أن النشاطات `A100 → A200 → A300 → A400` متصلة. أنشئ حدث «تأخر اعتماد الحفرية» بين `A100` و`A200` لمدة ثلاثة أيام. سيختار تبويب الأثر المالي مورد الحفار المسند إلى `A100` ومورد طاقم الأساسات المسند إلى `A200` لأنهما في نطاق اتصال الحدث. بعد ذلك أنشئ Notice مسودة، وعيّن مراجعين تجريبيين، وأكمل المراحل ثم صدّر التقرير.

> لا تنسخ تكاليف أو مدد المثال إلى مطالبة فعلية؛ المقصود هو التحقق من مسار الاستخدام فقط.

## مشروعان جديدان للتحقق القابل للتكرار

### 1. تدريب TIA — اعتماد أعمال الحفر

برنامج الملف `05` مدته الأساسية **17 يوم عمل**. عند إضافة Fragnet الملف `06` مكان العلاقة `TR-200` تصبح مدته **21 يوم عمل**؛ أي أثر شبكي مقداره **4 أيام عمل**. هذه نتيجة حسابية متوقعة لا إثبات لاستحقاق زمني أو مالي، وتُستخدم لاختبار أن المحرك يقرأ شبكة سليمة ويضيف الحدث في موضعه الصحيح.

### 2. تدريب تزامن — مساران حرجان متوازيان

برنامج الملف `07` مدته الأساسية **14 يوم عمل**، وفيه مساران متساويان يلتقيان قبل النشاط `C300`. يحتوي الملف `08` على حدث صاحب عمل وحدث مقاول، مدة كل واحد منهما 3 أيام، في مسارين متوازيين. بعد إدراج الحدثين، المدة المتوقعة هي **17 يوم عمل**؛ أي أن الأثر الزمني المدمج **3 أيام فقط** وليس مجموع المدد 6 أيام. المثال يوضح قاعدة حسابية عن التداخل، ولا يقسم المسؤولية ولا يقرر الاستحقاق التعاقدي.
