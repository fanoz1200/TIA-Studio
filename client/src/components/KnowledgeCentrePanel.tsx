import React, { useMemo, useRef, useState } from "react";
import { ArrowLeft, BookOpenCheck, CheckCircle2, ExternalLink, FileCode2, LibraryBig, Link2, LoaderCircle, LogIn, PlayCircle, Plus, Search, ShieldCheck, Trash2, Upload, Video } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import "./knowledge-centre.css";

type Track = "tia" | "concurrent" | "primavera";
export type AnalysisMethod = "tia" | "windows" | "disruption" | "quantity";
export type KnowledgeRoute = { method: AnalysisMethod; journeyPath: "issue" | "direct"; caseId: string; caseTitle: string };
const trackLabels: Record<Track, string> = { tia: "TIA من الصفر إلى التقرير", concurrent: "التأخيرات المتزامنة", primavera: "تطبيق Primavera P6" };
const methods: { id: AnalysisMethod; label: string; detail: string }[] = [
  { id: "tia", label: "تحليل الأثر الزمني TIA", detail: "للحدث المتوقع أو الجاري مع تحديث قبل الحدث وشبكة CPM قابلة للحساب." },
  { id: "windows", label: "تحليل النوافذ Windows", detail: "لمشروع مستمر يُراجع على فترات بتحديثات متتابعة، أو عند تداخل أكثر من حدث." },
  { id: "disruption", label: "تحليل التعطيل Disruption", detail: "لانخفاض الإنتاجية أو اضطراب التسلسل؛ الأدلة التشغيلية ضرورية بجانب أثر البرنامج." },
  { id: "quantity", label: "زيادة الكميات Quantity", detail: "لزيادة نطاق أو كمية بند؛ يفصل النظام الأثر الزمني عن التقييم التعاقدي والمالي." },
];
const cases: { id: string; title: string; terms: string[]; symptom: string; method: AnalysisMethod; reason: string; journeyPath: "issue" | "direct" }[] = [
  { id: "late-approval", title: "تأخر اعتماد الرسومات أو المستندات", terms: ["اعتماد", "رسومات", "shop drawing", "submittal", "موافقة", "مستند"], symptom: "العمل متوقف أو لا يبدأ انتظاراً لاعتماد أو توجيه.", method: "tia", reason: "تمثل مدة الانتظار Fragnet على التحديث السابق للحدث بعد مراجعة العلاقة الفعلية.", journeyPath: "direct" },
  { id: "change-quantity", title: "زيادة كميات أو تغيير نطاق", terms: ["زيادة", "كميات", "quantity", "variation", "تغيير", "نطاق", "25"], symptom: "كمية أو نطاق إضافي يؤثر في الموارد أو التسلسل أو الإكمال.", method: "quantity", reason: "توثق الواقعة والكمية، ثم تقاس المدة القابلة للإسناد قبل مناقشة الاستحقاق.", journeyPath: "issue" },
  { id: "late-access", title: "تأخر إتاحة جبهة أو موقع", terms: ["جبهة", "إتاحة", "تسليم", "site access", "access", "منطقة", "موقع"], symptom: "النشاط لا يبدأ لعدم تسليم منطقة العمل أو الوصول إليها.", method: "tia", reason: "يوضح Update قبل الحدث ونقطة الربط ما إذا كان التأثير حرجاً أو مستهلكاً للعائمة.", journeyPath: "direct" },
  { id: "concurrent", title: "تأخيران متزامنان", terms: ["متزامن", "concurrent", "تزامن", "طرفين", "employer", "contractor"], symptom: "حدثان أو سببان يتداخلان زمنياً وقد يمسان المسار الحرج.", method: "windows", reason: "تفصل النوافذ الفترات ومصادر التأثير، ثم تقرر المسؤولية تعاقدياً خارج الحساب الآلي.", journeyPath: "issue" },
  { id: "productivity", title: "تعطيل أو انخفاض إنتاجية", terms: ["انتاجية", "إنتاجية", "تعطيل", "disruption", "تباطؤ", "تداخل"], symptom: "العمل مستمر لكن إنتاجيته انخفضت أو تغير تسلسله.", method: "disruption", reason: "تحتاج الواقعة إلى قياس إنتاجية وأدلة تشغيلية؛ يقاس امتداد المسار فقط عند ثبوت التغيير.", journeyPath: "issue" },
  { id: "late-material", title: "تأخر توريد مادة أو معدة", terms: ["توريد", "مادة", "material", "supplier", "شراء", "معدات"], symptom: "مادة أو معدة حرجة لا تصل في الوقت اللازم للنشاط اللاحق.", method: "tia", reason: "تحدد العلاقة الفعلية والتحديث قبل الحدث الحرجية أو استهلاك العائمة.", journeyPath: "direct" },
];
const defaultTracks: { id: Track; title: string; description: string; outcome: string }[] = [
  { id: "tia", title: "مسار TIA العملي — Workshop 8", description: "من تثبيت Baseline وUpdate قبل الحدث إلى Fragnet وقياس أثر الإكمال مع اعتماد Pre / Event / Post.", outcome: "نتيجة قابلة للتتبع" },
  { id: "concurrent", title: "مسار التزامن", description: "تحديد الفترات المتداخلة وتمييز السببية والأثر الحرج قبل مراجعة الاستحقاق.", outcome: "مصفوفة تزامن قابلة للمراجعة" },
  { id: "primavera", title: "مسار Primavera P6", description: "تجهيز XER/XML وقراءة WBS والعلاقات والموارد قبل التحليل.", outcome: "بيانات جاهزة لـ CPM" },
];
function normalized(value: string) { return value.toLowerCase().replace(/[إأآ]/g, "ا").replace(/ى/g, "ي").replace(/ة/g, "ه").replace(/[^\w\u0600-\u06FF]+/g, " ").trim(); }
function base64Of(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onerror = () => reject(new Error("تعذر قراءة ملف المكتبة.")); reader.onload = () => resolve(String(reader.result)); reader.readAsDataURL(file); }); }
function formatBytes(value: number) { return value < 1_024 ? `${value} B` : value < 1_048_576 ? `${(value / 1_024).toFixed(1)} KB` : `${(value / 1_048_576).toFixed(1)} MB`; }

export function KnowledgeCentrePanel({ view, projectKey, isAuthenticated, onBeginGuidedAnalysis }: { view: string; projectKey: string; isAuthenticated: boolean; onBeginGuidedAnalysis?: (route: KnowledgeRoute) => void }) {
  const [query, setQuery] = useState(""); const [selectedCaseId, setSelectedCaseId] = useState(cases[0].id); const [selectedMethod, setSelectedMethod] = useState<AnalysisMethod>(cases[0].method);
  const [videoTitle, setVideoTitle] = useState(""); const [videoTrack, setVideoTrack] = useState<Track>("tia"); const [videoUrl, setVideoUrl] = useState(""); const [videoDescription, setVideoDescription] = useState("");
  const [libraryFile, setLibraryFile] = useState<File | null>(null); const [libraryTitle, setLibraryTitle] = useState(""); const [libraryVersion, setLibraryVersion] = useState(""); const [selectedDocumentId, setSelectedDocumentId] = useState<number | null>(null); const libraryInputRef = useRef<HTMLInputElement>(null);
  const queryInput = useMemo(() => ({ projectKey }), [projectKey]);
  const videos = trpc.knowledgeCentre.videoList.useQuery(queryInput, { enabled: isAuthenticated }); const documents = trpc.knowledgeCentre.libraryList.useQuery(queryInput, { enabled: isAuthenticated }); const documentContent = trpc.knowledgeCentre.libraryRead.useQuery({ id: selectedDocumentId ?? 0 }, { enabled: isAuthenticated && selectedDocumentId !== null });
  const refresh = () => { void videos.refetch(); void documents.refetch(); };
  const addVideo = trpc.knowledgeCentre.videoCreate.useMutation({ onSuccess: () => { setVideoTitle(""); setVideoUrl(""); setVideoDescription(""); refresh(); toast.success("تمت إضافة الرابط إلى المسار التعليمي."); } }); const removeVideo = trpc.knowledgeCentre.videoRemove.useMutation({ onSuccess: () => { refresh(); toast.success("تم حذف الرابط من مركز الفيديو."); } });
  const uploadLibrary = trpc.knowledgeCentre.libraryUpload.useMutation({ onSuccess: result => { setLibraryFile(null); setLibraryTitle(""); setLibraryVersion(""); refresh(); setSelectedDocumentId(result.id); toast.success("حُفظت نسخة مقروءة وآمنة من الموسوعة."); } }); const removeLibrary = trpc.knowledgeCentre.libraryRemove.useMutation({ onSuccess: () => { setSelectedDocumentId(null); refresh(); toast.success("تمت إزالة فهرس نسخة المكتبة من هذا المشروع."); } });
  const matches = useMemo(() => { const terms = normalized(query).split(" ").filter(Boolean); if (!terms.length) return cases; return cases.filter(item => { const haystack = normalized([item.title, item.symptom, item.reason, ...item.terms].join(" ")); return terms.every(term => haystack.includes(term)); }); }, [query]);
  const selectedCase = cases.find(item => item.id === selectedCaseId) ?? cases[0]; const selectedMethodInfo = methods.find(item => item.id === selectedMethod) ?? methods[0]; const selectedDocument = documents.data?.find(item => item.id === selectedDocumentId);
  const selectCase = (item: typeof cases[number]) => { setSelectedCaseId(item.id); setSelectedMethod(item.method); };
  const startAnalysis = () => onBeginGuidedAnalysis?.({ method: selectedMethod, journeyPath: selectedCase.journeyPath, caseId: selectedCase.id, caseTitle: selectedCase.title });
  const addLibrary = async () => { if (!libraryFile || !libraryTitle.trim()) { toast.error("اختر ملف HTML وأدخل عنواناً واضحاً للنسخة."); return; } try { uploadLibrary.mutate({ projectKey, title: libraryTitle.trim(), versionLabel: libraryVersion.trim() || undefined, fileName: libraryFile.name, dataBase64: await base64Of(libraryFile) }); } catch (error) { toast.error(error instanceof Error ? error.message : "تعذر تجهيز ملف المكتبة."); } };
  if (view !== "learning") return null;
  return <section className="knowledge-centre workflow-panel"><div className="workflow-heading"><div><p className="eyebrow">LEARNING → METHOD → TIA</p><h2>الموسوعة وبوابة اختيار الحل</h2><p>اكتب وصف الواقعة، راجع المنهج المقترح وسببه، ثم ابدأ إدخال بياناتك بترتيب Workshop 8. البحث يرشدك ولا يصدر حكماً تعاقدياً تلقائياً.</p></div><LibraryBig size={24} /></div>
    <article className="case-search-gate"><div className="case-search-header"><span><Search size={18} />ابحث عن المشكلة كما حدثت في المشروع</span><b>الخطوة 1 من الرحلة الجديدة</b></div><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="مثال: تأخر اعتماد رسومات، زيادة كميات، توريد، تعطيل إنتاجية…" /><div className="case-result-grid">{matches.length ? matches.map(item => <button type="button" key={item.id} onClick={() => selectCase(item)} className={`case-result ${item.id === selectedCase.id ? "selected" : ""}`}><b>{item.title}</b><span>{item.symptom}</span><em>{methods.find(method => method.id === item.method)?.label}</em></button>) : <p className="case-empty">لا توجد نتيجة مباشرة. اختر المنهج الأقرب وسجّل الواقعة وأدلتها للمراجعة.</p>}</div><div className="method-selection"><div><Label>المنهج المقترح أو المختار</Label><Select value={selectedMethod} onValueChange={value => setSelectedMethod(value as AnalysisMethod)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{methods.map(item => <SelectItem key={item.id} value={item.id}>{item.label}</SelectItem>)}</SelectContent></Select></div><div className="method-rationale"><b>{selectedMethodInfo.label}</b><p>{selectedMethodInfo.detail}</p><small><CheckCircle2 size={14} />{selectedMethod === selectedCase.method ? selectedCase.reason : "يمكن تغيير المنهج بعد مراجعة العقد والأدلة؛ سيظهر سبب الاختيار داخل ملف التحليل."}</small></div></div><div className="case-search-actions"><span><ShieldCheck size={16} />تبقى ملفات XER والموسوعة الأصلية للقراءة فقط ولا يعدّلها التطبيق.</span><Button className="run-button" onClick={startAnalysis}>طبّق هذا المنهج الآن <ArrowLeft size={16} /></Button></div></article>
    <div className="learning-path-grid">{defaultTracks.map(track => <article className="learning-path" key={track.id}><span><PlayCircle size={18} />{trackLabels[track.id]}</span><h3>{track.title}</h3><p>{track.description}</p><b>{track.outcome}</b><div className="learning-path-actions"><Button className="run-button" size="sm" onClick={() => { setSelectedMethod(track.id === "concurrent" ? "windows" : "tia"); startAnalysis(); }}>ابدأ المسار <ArrowLeft size={14} /></Button><Button variant="outline" size="sm" onClick={() => document.getElementById("video-library-form")?.scrollIntoView({ behavior: "smooth", block: "center" })}><Plus size={15} />إضافة فيديو</Button></div></article>)}</div>
    {!isAuthenticated ? <div className="workflow-login"><LogIn size={18} /><span>سجّل الدخول فقط لإضافة روابط التدريب أو رفع نسخة موسوعة خاصة بالمشروع. البحث وبداية التحليل متاحان أعلاه.</span><Button className="run-button" onClick={startLogin}>تسجيل الدخول</Button></div> : <><div className="knowledge-layout"><article className="knowledge-card"><div className="knowledge-card-heading"><div><Video size={20} /><h3>مكتبة الفيديو المنظمة</h3></div><span>روابط خارجية فقط</span></div><p>أضف روابط HTTPS من YouTube أو Vimeo بعد مراجعة ملاءمتها المرجعية.</p><div className="video-add-form" id="video-library-form"><div><Label>عنوان الدرس</Label><Input value={videoTitle} onChange={event => setVideoTitle(event.target.value)} placeholder="مثال: إدخال Fragnet داخل P6" /></div><div><Label>المسار</Label><Select value={videoTrack} onValueChange={value => setVideoTrack(value as Track)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{(Object.keys(trackLabels) as Track[]).map(track => <SelectItem key={track} value={track}>{trackLabels[track]}</SelectItem>)}</SelectContent></Select></div><div><Label>رابط الفيديو</Label><Input dir="ltr" type="url" value={videoUrl} onChange={event => setVideoUrl(event.target.value)} placeholder="https://www.youtube.com/..." /></div><div className="video-description"><Label>ملاحظة تعليمية</Label><Textarea value={videoDescription} onChange={event => setVideoDescription(event.target.value)} placeholder="ما الذي يغطيه الفيديو؟" /></div><Button className="run-button" disabled={addVideo.isPending || !videoTitle.trim() || !videoUrl.trim()} onClick={() => addVideo.mutate({ projectKey, title: videoTitle.trim(), track: videoTrack, videoUrl: videoUrl.trim(), description: videoDescription.trim() || undefined })}>{addVideo.isPending ? <LoaderCircle className="spin" size={16} /> : <Link2 size={16} />}إضافة الرابط</Button></div><div className="tutorial-list">{videos.isLoading ? <p>جار تحميل روابط الدروس…</p> : videos.data?.length ? videos.data.map(video => <div className="tutorial-row" key={video.id}><div><span>{trackLabels[video.track]}</span><b>{video.title}</b>{video.description ? <small>{video.description}</small> : null}</div><div><Button size="sm" variant="outline" onClick={() => window.open(video.videoUrl, "_blank", "noopener,noreferrer")}><ExternalLink size={15} />فتح الدرس</Button><Button size="icon" variant="ghost" aria-label={`حذف ${video.title}`} disabled={removeVideo.isPending} onClick={() => removeVideo.mutate({ id: video.id })}><Trash2 size={16} /></Button></div></div>) : <p className="workflow-subtle">لا توجد روابط بعد.</p>}</div></article><article className="knowledge-card library-card"><div className="knowledge-card-heading"><div><BookOpenCheck size={20} /><h3>مكتبة منهجيات HTML</h3></div><span>قراءة آمنة</span></div><p>تُزال العناصر النشطة قبل الحفظ. الأصل لا يدخل قاعدة البيانات ولا يُعدّل؛ تحفَظ نسخة قراءة معقّمة فقط عند موافقتك الصريحة هنا.</p><div className="library-upload-form"><input ref={libraryInputRef} className="sr-only" type="file" accept=".html,.htm,text/html" onChange={event => { const next = event.target.files?.[0] ?? null; setLibraryFile(next); if (next && !libraryTitle) setLibraryTitle(next.name.replace(/\.html?$/i, "")); }} /><div><Label>نسخة الموسوعة</Label><Button type="button" variant="outline" className="file-picker" onClick={() => libraryInputRef.current?.click()}><FileCode2 size={16} />{libraryFile ? libraryFile.name : "اختيار HTML"}</Button></div><div><Label>عنوان النسخة</Label><Input value={libraryTitle} onChange={event => setLibraryTitle(event.target.value)} placeholder="الموسوعة المرجعية للتأخيرات" /></div><div><Label>رقم/تاريخ الإصدار</Label><Input value={libraryVersion} onChange={event => setLibraryVersion(event.target.value)} placeholder="إصدار 1.0" /></div><Button className="run-button" disabled={uploadLibrary.isPending || !libraryFile || !libraryTitle.trim()} onClick={() => void addLibrary()}>{uploadLibrary.isPending ? <LoaderCircle className="spin" size={16} /> : <Upload size={16} />}رفع نسخة القراءة</Button></div><div className="library-list">{documents.isLoading ? <p>جار تحميل نسخ المكتبة…</p> : documents.data?.length ? documents.data.map(document => <div className={`library-row ${selectedDocumentId === document.id ? "selected" : ""}`} key={document.id}><button type="button" onClick={() => setSelectedDocumentId(document.id)}><b>{document.title}</b><span>{document.versionLabel || "دون إصدار"} · {formatBytes(document.sizeBytes)} · {new Date(document.createdAt).toLocaleDateString("ar-EG")}</span></button><Button size="icon" variant="ghost" aria-label={`حذف ${document.title}`} disabled={removeLibrary.isPending} onClick={() => removeLibrary.mutate({ id: document.id })}><Trash2 size={16} /></Button></div>) : <p className="workflow-subtle">لم تُرفع موسوعة بعد.</p>}</div></article></div>{selectedDocumentId ? <article className="safe-reader"><div><div><ShieldCheck size={20} /><h3>{selectedDocument?.title || "عارض المكتبة"}</h3></div><span>iframe sandbox · لا JavaScript</span></div>{documentContent.isLoading ? <p>جار تجهيز نسخة القراءة…</p> : documentContent.error ? <p className="analysis-error">{documentContent.error.message}</p> : <iframe title={`قراءة ${selectedDocument?.title || "المكتبة"}`} sandbox="" referrerPolicy="no-referrer" srcDoc={documentContent.data?.sanitizedHtml || ""} />}</article> : null}</>}<p className="knowledge-boundary"><ShieldCheck size={16} />هذا المركز مرجعي فقط؛ نتيجة TIA تستخرج من بيانات البرنامج وحساب CPM الموثق، لا من نص أو فيديو منفرد.</p></section>;
}
