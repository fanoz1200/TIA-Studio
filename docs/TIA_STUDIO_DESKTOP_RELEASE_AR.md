# TIA Studio — إصدار سطح المكتب 1.0.7

## الغرض الحقيقي من نسخة سطح المكتب

تغلف نسخة سطح المكتب منصة **TIA Studio** داخل تطبيق Electron محلي. يبدأ التطبيق خادماً على `127.0.0.1` فقط ثم يفتح الواجهة في نافذة مستقلة؛ لا يفتح المنفذ على الشبكة المحلية ولا يمنح الواجهة صلاحية لتنفيذ أوامر نظام التشغيل.

> **الحدود الواضحة:** تعمل قراءة XER/XML، عارض XER المحلي، حساب CPM/TIA، التقويم الإقليمي، بوابة الجودة، والقوالب والتقارير محلياً. أما تسجيل الدخول والسجل المشترك والتخزين السحابي والإشعارات فتحتاج خدمة المنصة وبيانات اعتماد صحيحة. الاستخدام **دون اتصال** متاح للوظائف المحلية والواجهة المخزنة بعد أول تشغيل مناسب، وليس بديلاً عن خدمات الحساب.

## التنزيل والتشغيل

لا تحتاج إلى Node.js أو أوامر أو فك ملف ZIP. حمّل ملفاً واحداً مناسباً لنظامك وافتحه بنقرتين.

| النظام | رابط التنزيل الرسمي | تستخدمه إمتى؟ |
|---|---|---|
| Windows x64 — **Setup (موصى به)** | [`TIA-Studio-1.0.7-Windows-x64-Setup.exe`](/manus-storage/TIA-Studio-1.0.7-Windows-x64-Setup_7327be86.exe) | للتثبيت الحقيقي: ينشئ مدخلاً في Start وقائمة إزالة التثبيت. حمّله فقط لو ستثبّت البرنامج. |
| Windows x64 — **Portable** | [`TIA-Studio-1.0.7-Windows-x64-Portable.exe`](/manus-storage/TIA-Studio-1.0.7-Windows-x64-Portable_20a0a969.exe) | للتجربة أو التشغيل المباشر بلا تثبيت. افتحه بالنقر المزدوج ولا يتوقع منه إنشاء أيقونة Start. |
| Linux x64 | [`TIA-Studio-1.0.7-Linux-x64.AppImage`](/manus-storage/TIA-Studio-1.0.7-Linux-x64_8ef18634.AppImage) | تشغيل محلي بعد جعله قابلاً للتنفيذ. |
| المصدر والتسليم | [`TIA-Studio-1.0.7-Source.tar.gz`](/manus-storage/TIA-Studio-1.0.7-Source.tar_2eb10450.gz) | أرشيف للمصدر والأدلة، من دون XER/XLSX خام أو أسرار. |

> الحزم غير موقعة رقمياً. لا تعطل الحماية بشكل عام ولا تستخدم رابطاً مجهولاً. افحص الملف ببرنامج الحماية ثم افتحه. لو ظهر SmartScreen على Windows، تحقق من اسم الملف والبصمة أدناه ومن مصدر التنزيل قبل اتخاذ أي قرار.

| الملف | SHA-256 |
|---|---|
| Windows Setup | `15dcc0c6c5523fe36c6363b6c0f09ab69afa46210f8b019f9922e10767cdbd83` |
| Windows Portable | `c06896df092c43fafdfe8b788a679a3ca6ad91b033f498f4f33c2d24ff6ce224` |
| Linux AppImage | `3668d4ef09e192ec4e2ba9f7b50bf89aab999bdc6ce8e72f0284dbab480249b6` |
| Source TAR.GZ | `fde9dbadea904b5142594eccdfff8ef8ab95f8dad4f225af43f1b5217f3b3d4d` |

> **فهم النسخة الظاهرة في Start:** وجود أيقونة في Start ونافذة مستقلة يرجّح أن النسخة المثبتة تطبيق سطح مكتب أو تطبيق ويب مثبّت، لكنه لا يثبت نوعها بدقة من الصورة وحدها. لو النسخة الحالية تعمل عندك، فهي مسار تشغيل قائم، لكن لا تحذفها قبل تجربة Setup الجديد أو تأكيد عمل النسخة المحمولة الجديدة.

في Linux اجعل الملف قابلاً للتشغيل مرة واحدة، ثم افتحه بالنقر المزدوج. عند إغلاق النافذة يتوقف الخادم المحلي؛ لا توجد خدمة دائمة في الخلفية.

## ما أضيف في 1.0.7

مصر هي الإعداد المبدئي للتقويم: ستة أيام عمل من السبت إلى الخميس. تستطيع اختيار مصر أو دول الخليج أو دول عربية أخرى، ثم تحميل اقتراح إجازات **يدوياً** ومراجعة مصدره وتاريخ فحصه قبل الاعتماد. لا تعالج الأداة الإجازات الهجرية أو الترحيلات كحكم تلقائي؛ يجب مراجعة تقويم العقد والمشروع.

أضيفت بوابة جودة إلزامية بعد رفع Baseline وUpdate وقبل إدخال واقعة أو إنشاء Fragnet. تفحص الرحلة الأنشطة والعلاقات وData Date والتقويم بشكل واضح. كما يدعم سجل القضايا البحث والاختيار المتعدد ولصق Activity IDs من Excel؛ كل نقطة ربط Fragnet تبقى مستقلة للمراجعة.

أضيف عارض XER محلي للقراءة فقط يعرض الأنشطة والعلاقات وWBS والتقويم وفروق النسخ قبل الرجوع إلى Primavera. أضيف أيضاً قالب Excel للوقائع فيه حقول إجبارية واختيارية وقوائم وتحقق إدخال وتعليمات، مع تنزيل مسودة Notice محلية من دون تسجيل دخول؛ الحفظ والسجل المشترك يظلان داخل الحساب.

## التحقق الذي تم

بعد إصلاح حزمة Windows، اجتازت المنصة **142 اختباراً في 46 ملف اختبار**، وفحص TypeScript وبناء الإنتاج. أضيفت اختبارات تغليف سطح المكتب ضمن المجموعة وفحص يمنع إدراج Vite في خادم الإنتاج. راجعت الواجهات الأساسية على سطح المكتب والهاتف. ملف Windows Portable شُغّل داخل Wine/Xvfb بحساب مؤقت، وبدأ خادمه المحلي واستجاب من المنفذ **4317**. كما بُني Setup قياسي بصيغة NSIS مع منع فتح البرنامج تلقائياً بعد التثبيت الصامت.

الإصلاح يعالج خطأ `Cannot find package 'vite'` عند تشغيل الحزمة المحمولة من `%LocalAppData%\Temp`؛ صار خادم الإنتاج منفصلاً عن Vite الذي يخص التطوير فقط. لا تستخدم أي ملف Windows قديم من 1.0.7: استخدم **Setup** أو **Portable** المذكورين في الجدول فقط.

هذا فحص إقلاع وعرض محلي فقط، وهو تحقق **تجريبي محدود النطاق** وليس اعتماداً مهنياً أو فحص توافق لكل خصائص Windows. فحص Portable نجح داخل Wine؛ أما تثبيت Setup داخل Wine فتعذر حسمه بسبب استقرار بيئة الاختبار نفسها، لذلك يلزم قبل الاعتماد فتح Setup على جهاز Windows حقيقي بحساب مستخدم عادي، والتحقق من SmartScreen والحماية والطباعة وفتح مشروع تدريبي واستيراده وتشغيل TIA وتصدير التقرير وExcel.

## حدود Primavera والمطالبات

لا يدّعي محرك TIA المحلي تكافؤ Primavera P6. قبل استعمال أي نتيجة في مطالبة أو قرار زمني، أعد استيراد ملف التصدير في نسخة **غير إنتاجية** من P6 Professional 23.12، وشغّل F9، ثم راجع Calendar وFloat وCritical Path والعلاقات وتاريخ النهاية والتقويم التعاقدي. لا تشكّل التقارير أو مسودة Notice حكماً قانونياً أو إثبات استحقاق تلقائياً؛ يلزم مراجعة مختص ومراسلات المشروع الفعلية.

## الإنشاء للمطورين

يتطلب الإنشاء Node.js 22 أو أحدث وpnpm:

```bash
pnpm install --frozen-lockfile
pnpm test
pnpm check
pnpm build
pnpm desktop:pack:windows
pnpm desktop:verify:windows-sandbox
```

لا تضف ملفات مشاريع حقيقية أو أسرار أو `.env` إلى حزمة الإصدار أو مستودع Git. يبقى XER المصدر للقراءة فقط، وأي XER صادر تجريبي يجب أن يمر بالتحقق العكسي والاستيراد اليدوي في P6 غير إنتاجي.
