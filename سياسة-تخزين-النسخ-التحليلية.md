# سياسة تخزين وحوكمة النسخ التحليلية

**الحالة:** سياسة تشغيل للإصدار الحالي من TIA Studio  
**النطاق:** ملفات البرنامج المصدرية، النسخ التحليلية، أدلة الحدث، وتقارير المطالبة  
**آخر مراجعة:** 18 أغسطس 2026

> **قاعدة الحوكمة:** لا يُعدَّل ملف برنامج المستخدم الأصلي ولا تُستبدل بياناته. كل تحليل TIA يُنشئ لقطة مستقلة قابلة للمراجعة، وتظل الأدلة والنتائج والتقارير مرتبطة بمعرّف التحليل لا بملف مصدر قابل للكتابة.

## 1. نموذج الفصل بين أنواع البيانات

| فئة الأصل | الوضع | موقع الحفظ الحالي | سياسة التعديل | الغرض |
| --- | --- | --- | --- | --- |
| ملف XER أو XML الأصلي | قراءة فقط | ذاكرة المتصفح أثناء جلسة الاستيراد؛ ويبقى الملف الذي اختاره المستخدم خارج قاعدة البيانات افتراضياً. | لا يُعدّل ولا يُستبدل. | تثبيت مصدر البرنامج الذي عُرض للتحليل. |
| نسخة `Pre-TIA` | لقطة تحليلية | كائن بيانات جدولة مستقل داخل جلسة التطبيق وتصدير JSON عند طلب المستخدم. | لا تُعدّل بعد اعتمادها؛ أي تعديل ينشئ تحليلاً جديداً. | مرجع تاريخ الإكمال قبل الحدث. |
| نسخة `Post-TIA` | لقطة تحليلية | كائن بيانات مستقل عن المصدر و`Pre-TIA` داخل الجلسة وتصدير JSON عند طلب المستخدم. | لا تُعدّل الأصل؛ إعادة النمذجة تنشئ نسخة لاحقة. | إثبات أثر الـFragnet بعد الإدراج. |
| Fragnet وتقسيم `Pre/Event/Post` | سجل نموذج | جزء من نسخة `Post-TIA` ومن سجل تحليل الحدث. | يخضع للمراجعة والاعتماد قبل التقرير. | بيان منطق السببية ونقاط الربط. |
| أدلة الحدث | ملف وسجل بيانات | تخزين كائنات منفصل مع بيانات وصفية في قاعدة التطبيق. | يضاف أو يفصل عن الحدث مع سجل تدقيق؛ لا يغيّر الملف المصدر. | سند الوقائع والمراسلات والبرهان. |
| Notice ومسار الاعتماد | سجل أعمال | قاعدة البيانات مع أثر تدقيقي. | لا يعدّل قراراً سابقاً؛ ينشئ قراراً أو تعليقاً جديداً. | إثبات المراجعة والمسؤولية. |
| تقرير Word/PDF وJSON | ناتج قابل للتصدير | ينشأ بناء على اللقطات المعتمدة عند طلب المستخدم. | لا يعيد كتابة اللقطات التي بُني عليها. | مشاركة النتيجة ومراجعتها خارج التطبيق. |

## 2. معرّف التحليل وسلسلة الحفظ

ينبغي أن يحمل كل تحليل رقم مطالبة أو معرفاً داخلياً، ومعرف ملف المصدر الذي اختاره المستخدم، ومعرّف نسخة `Pre-TIA`، ومعرّف نسخة `Post-TIA`، ومعرّف الـFragnet أو نشاط التقسيم. تحفظ هذه المراجع في سجل المطالبة وسجل الاعتماد وسجل الـNotice كلما استُخدمت. وبهذا يستطيع المراجع معرفة أي برنامج سابق للحدث، وأي نموذج أُدرج بعده، وأي نتيجة ظهر منها التقرير.

لا يفترض النظام أن استيراد الملف يعني نقل ملكيته إلى قاعدة البيانات. فالأدلة وحدها تستخدم تخزين ملفات مخصصاً عند رفع المستخدم لها؛ أما البرنامج المصدر والنسخ التحليلية في بقائها الحالي محلية داخل المتصفح أو في ملف JSON يختاره المستخدم للتنزيل والحفظ. هذا الفصل يحقق مطلب القراءة فقط للمواد المرفقة ويقلل خطر خلط بيانات مشروع فعلي بمراجع تدريبية.

## 3. دورة حياة التحليل

| المرحلة | الإدخال | المخرج المسموح | حاجز القرار |
| --- | --- | --- | --- |
| الاستيراد | XER أو XML أو JSON يختاره المستخدم. | عدادات قراءة وملاحظات الاستيراد. | بوابة P6 تمنع الانتقال إذا كانت بيانات CPM الجوهرية غير كافية. |
| تجميد المرجع | برنامج المصدر المقروء. | لقطة `Pre-TIA` مستقلة. | اعتماد صريح للنسخة المرجعية قبل فتح نموذج الحدث. |
| نمذجة الحدث | حدث موثق مع نشاط متأثر وFragnet. | لقطة `Post-TIA` ونتيجة مقارنة. | فحوص شبكية وفحوص نتيجة TIA تصنف قبولاً أو تحذيراً أو رفضاً. |
| التقرير | نتيجة غير مرفوضة، أدلة، مراجعة. | JSON وWord وPDF عند الحاجة. | بوابة التصدير تمنع التقرير إذا ظهر مانع حسابي. |
| التتابع | مطالبة/تحليل لاحق. | رابط إلى نتيجة أو مطالبة سابقة. | يمنع النظام حلقات سلسلة المطالبات ويحافظ على منهجية متسقة. |

## 4. النسخ، الاستعادة، والتغيير

عند تغيير تاريخ الحدث أو النشاط المتأثر أو علاقات الـFragnet أو التقويم، لا يعاد استخدام `Post-TIA` السابق بوصفه الحقيقة الجديدة. ينشأ تحليل جديد أو لقطة جديدة تحمل سبب التغيير وتاريخاً ومراجع الأدلة. تبقى النتيجة السابقة قابلة للقراءة ما لم يتخذ فريق المطالبة قراراً بسحبها أو استبدالها في السجل. ويمنع ذلك تحويل تعديل تجريبي إلى أثر زمني نهائي من غير أثر تدقيقي.

التصدير JSON هو حزمة النقل المسموح بها حالياً للنسخ التحليلية. لا يوجد مُصدّر XER في هذا الإصدار؛ لذا لا ينبغي تغيير امتداد JSON أو تقديمه للمستخدم على أنه ملف يمكن فتحه في Primavera. عندما يضاف مُصدّر XER مستقبلاً، سيكون ناتجه ملفاً جديداً موسوماً مثل `Project-Pre-TIA.xer` أو `Project-Post-TIA.xer`، ويخضع لفتح تجريبي في Primavera ومقارنة عدد الأنشطة والعلاقات والتقاويم والموارد قبل اعتماده.

## 5. حدود الحفظ الحالية وخطة التطوير التالية

| الوضع الحالي | التحكم القائم | التطوير المقترح قبل الاعتماد المؤسسي |
| --- | --- | --- |
| لقطات التحليل محلية داخل الجلسة مع تنزيل JSON اختياري. | المصدر لا يُكتب تلقائياً في قاعدة البيانات. | إضافة سجل ميتاداتا للقطات بإذن صريح، مع ملف JSON أو أصل مشفر في تخزين كائنات منفصل. |
| الأدلة تحفظ منفصلة مع سجل ربط بالحدث. | مراجعة الصلاحيات وأثر التدقيق متاحان لمسار المطالبة. | سياسة احتفاظ، تصنيف سرية، وتحكم تنزيل حسب الدور. |
| التقرير يولّد عند الطلب من حالة التحليل الحالية. | بوابة الجودة تمنع التصدير عند الرفض الحسابي. | حفظ بصمة/تجزئة لحزمة التقرير واللقطات المستند إليها لتسهيل التحقق الخارجي. |
| لا يوجد XER صادر. | لا يوجد خطر إعادة كتابة ملف P6 أو تقديم ملف غير مكتمل على أنه P6. | بناء مُصدّر XER تجريبي مع اختبارات استيراد/فتح مستقلة في Primavera قبل الإعلان عن دعمه. |

## 6. ضوابط عملية للمستخدم

ينبغي للمحلل أن يحتفظ بنسخة ملف P6 الأصلية في نظام إدارة مستندات المشروع وأن يسجل اسمها وإصدارها وتاريخ البيانات في ملف القضية. بعد اعتماد `Pre-TIA`، يجب تنزيل JSON الخاص بالتحليل عند الحاجة إلى مراجعة مستقلة، وإرفاق مراجع الأدلة والـNotice في السجل، وعدم استبدال التقرير النهائي بنتيجة تجريبية. ويظل قرار الاستحقاق والتعويض خاضعاً للعقد والوقائع والمراجعة المختصة، حتى لو اجتازت النتيجة كل فحوص المحرك.
