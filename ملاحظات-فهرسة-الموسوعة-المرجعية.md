# ملاحظات فهرسة الموسوعة المرجعية

## المصدر والحماية

المصدر هو الملف الذي أرسله المستخدم: `/home/ubuntu/upload/Master_Claim_Intelligence_HTML.html`. هذا الملف **لا يُعدَّل ولا يُنقل إلى قاعدة البيانات**. سيُستخرج منه فهرس عرض محلي ثابت داخل التطبيق، بينما يبقى الأصل مرجعاً للقراءة فقط.

## البنية التي تم التحقق منها

يحمل الملف عنوان **Master Claim Intelligence — نظام ذكاء المطالبات الإنشائية V3.0**. يحتوي على شبكة حالات تبدأ من `D-001`، وسجل بيانات أصلي تحت `window.CASES_DATA` يبدأ قرب السطر 13540. كل حالة تتضمن الحقول التالية:

| الحقل | الغرض في المكتبة الجديدة |
|---|---|
| `case_id` | الرقم المرجعي للحالة، مثل `D-001` |
| `title_ar` و`title_en` | عنوان الحالة بالعربية والإنجليزية |
| `category` و`delay_type` | التصنيف ونوع/مصدر التأخير |
| `methodology` | المنهج المرجح، مثل TIA أو Windows |
| `description` | وصف الحالة وسياقها العملي |
| `root_cause` | الأسباب الجذرية |
| `schedule_impact` | الأثر المتوقع على البرنامج أو المسار الحرج |
| `contractual_basis` | المساندات التعاقدية المرجعية، ومنها FIDIC عند ورودها في المصدر |
| `burden_of_proof` | الأدلة والمستندات المطلوبة لإثبات الواقعة |

## أمثلة مؤكدة من المصدر

تتضمن الحالات المبكرة: تأخير اعتماد اللوحات الإنشائية التنفيذية، تأخير اعتماد لوحات MEP، الرفض المتكرر للوحات التنفيذية، تأخير اعتماد عينات المواد، وتأخير الرد على RFI. هذه ليست بيانات مصطنعة؛ بل ستُستخرج من السجل الأصلي وتظهر مع حقولها الكاملة في واجهة القراءة.

## قرار التنفيذ

ستعرض المكتبة بطاقات لجميع الحالات المستخرجة، بحثاً عربياً وإنجليزياً، وتصنيفات ومنهجاً مرجعياً. ويكشف اختيار الحالة شرحها والأدلة والأثر والمساندات التي وردت في المصدر، مع زر يفتح رحلة التحليل ويُمرّر رقم الحالة والمنهج **من دون اتخاذ قرار تعاقدي آلي**.

## ملاحظة تحديث — ملف Excel المرجعي

تم فحص `/home/ubuntu/upload/Master_Claim_Intelligence.xlsx` للقراءة فقط. تحتوي الورقة `02_Case_Library` على رأس جدول كامل يبدأ في الصف السابع ويشمل رقم الحالة، العنوان العربي والإنجليزي، التصنيف، نوع التأخير، المنهجية، الوصف، السبب الجذري، الأثر، الحل، الوقاية، الأساس التعاقدي، وبروتوكول Fragnet وقواعد TIA والتقويم والـFloat وعبء الإثبات وإجراء التحديث والاستدراك. النص التعريفي في المصدر يذكر 70 حالة، لكن عدّ السجلات ذات معرّفات `D-...` في الجدول الفعلي أظهر 55 سجلاً فقط، من `D-001` إلى `D-055`.

تم فحص بقية ملفات Excel المرجعية المرفوعة كذلك؛ ولم يظهر فيها سجل حالات إضافي بالمُعرّف نفسه. ستعرض المكتبة كل السجلات الفعلية من Excel وتطابقها مع HTML عند وجود تفاصيل إضافية، من دون اختلاق الحالات 56–88. إذا كانت هذه الحالات في ملف منفصل، يلزم إرفاقه لاستيرادها بأمان.
